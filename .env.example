# AccessFlow SDK — GCP Artifact Registry (package install)
GCP_SDK_NPM_REGISTRY_URL=us-east1-npm.pkg.dev/accessflow-rm5h5j8lmu/stag-accessflow-npm
ACCESSFLOW_NPM_REGISTRY_TOKEN=

# AccessFlow SDK — runtime Audit SDK API key (generate in accessFlow → Profile → Token management)
# Do not use the NPM install token here. Do not prefix with "export".
AF_NODE_PACKAGE_KEY=
